export enum Page {
  Root = 'root',
  ComponentDetails = 'component-details',
  Components = 'components',
  Theme = 'theme',
}
export enum Component {
  Button = 'button',
  Badge = 'badge',
  Checkbox = 'checkbox',
}
